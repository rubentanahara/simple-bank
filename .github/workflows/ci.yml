name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:12-alpine
        env:
          POSTGRES_USER: postgres-user
          POSTGRES_PASSWORD: postgres-password
          POSTGRES_DB: simple_bank
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Download dependencies
        run: go mod download

      - name: Generate sqlc code
        run: make sqlc
        shell: bash

      - name: Check for changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "Detected uncommitted changes after make sqlc. Run 'make sqlc' locally."
            exit 1
          fi

      - name: Run unit tests
        run: go test ./... -count=1

      - name: Run tests with race detector
        run: go test -race ./... -count=1

      - name: Run migrations
        env:
          POSTGRES_USER: postgres-user
          POSTGRES_PASSWORD: postgres-password
        run: make db-up
        shell: bash

      - name: Run integration tests
        run: make test-integration

      - name: Run coverage
        run: make cover

